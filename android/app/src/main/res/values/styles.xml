<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window theme applied while the Android process is starting -->
    <style name="LaunchTheme" parent="@android:style/Theme.Translucent.NoTitleBar.Fullscreen">
        <!-- Transparent window for no splash -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Default theme for your application after launch -->
    <style name="NormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <!-- Alias SplashScreen to our transparent LaunchTheme -->
    <style name="Theme.SplashScreen" parent="@style/LaunchTheme"/>
</resources>
